export default class ETPDate {
    day: number;
    month: string;
    year: number;

    constructor(day: number, month: string, year: number){
        this.day = day;
        this.month = month;
        this.year = year;
    }
}